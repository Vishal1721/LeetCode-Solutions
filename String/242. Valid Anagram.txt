class Solution {
public:
    bool isAnagram(string s, string t) {
       vector<int>a(256,0);
       for(char ch:s)
       {
        a[ch]++;
       }
       for(char ch: t)
       {
        if(a[ch]==0)
        {
            return false;
        }
        
       }
       return true;
    }
};